# AD_RANK 광고 시스템 Elasticsearch 구현 체크리스트

## 📋 목차
1. [개발 목표 및 성능 목표](#-개발-목표-및-성능-목표)
2. [인덱스 설계 및 생성](#-인덱스-설계-및-생성)
3. [하이브리드 아키텍처 구현](#-하이브리드-아키텍처-구현)
4. [적응형 조회 전략 구현](#-적응형-조회-전략-구현)
5. [성능 최적화](#-성능-최적화)
6. [운영 및 모니터링](#-운영-및-모니터링)
7. [백업 및 복구](#-백업-및-복구)

---

## 🎯 개발 목표 및 성능 목표

### **개발 목표**
- [ ] **적응형 랭킹 조회**: 데이터 크기에 따른 최적 조회 방식 자동 선택
- [ ] **실시간 업데이트**: 예산/잔액 소진 시 즉시 반영
- [ ] **배치 업데이트**: 일반 변경사항 15분마다 반영
- [ ] **고가용성**: 99.9% 이상 가동률 보장

### **성능 목표**
- [ ] **전체 평균 응답시간**: < 5ms
- [ ] **Elasticsearch O(1) 조회**: < 3ms
- [ ] **Redis 캐시 조회**: < 3ms
- [ ] **실시간 계산**: < 10ms
- [ ] **혼합 처리량**: 6000 QPS
- [ ] **캐시 히트율**: 85% 이상

### **기술 스택**
- [ ] **Elasticsearch**: 8.x 버전
- [ ] **클러스터 구성**: 3 Master + 3 Data Node
- [ ] **Java Client**: Elasticsearch Java API Client
- [ ] **모니터링**: Kibana + Elasticsearch Watcher

---

## 📊 인덱스 설계 및 생성

### **✅ 1. 인덱스 템플릿 생성**

**인덱스 템플릿 설정**:
```json
PUT _template/ad_rankings_template
{
  "index_patterns": ["ad_rankings_*"],
  "settings": {
    "number_of_shards": 4,
    "number_of_replicas": 1,
    "refresh_interval": "1s",
    "index.max_result_window": 10000,
    "index.mapping.total_fields.limit": 2000
  },
  "mappings": {
    "properties": {
      "campaign_id": { "type": "long" },
      "advertiser_id": { "type": "long" },
      "product_id": { "type": "long" },
      "placement": { 
        "type": "keyword",
        "fields": {
          "text": { "type": "text", "analyzer": "standard" }
        }
      },
      "category_path": { 
        "type": "keyword",
        "fields": {
          "text": { "type": "text", "analyzer": "standard" }
        }
      },
      "search_term": { 
        "type": "keyword",
        "fields": {
          "text": { "type": "text", "analyzer": "standard" }
        }
      },
      "ad_rank_score": { "type": "double" },
      "cpc_price": { "type": "double" },
      "exposure_rank": { "type": "integer" },
      "exposure_weight": { "type": "double" },
      "random_weight": { "type": "double" },
      "click_conversion_rate": { "type": "double" },
      "max_bid_price": { "type": "double" },
      "daily_budget": { "type": "double" },
      "current_spent": { "type": "double" },
      "wallet_balance": { "type": "double" },
      "campaign_status": { "type": "keyword" },
      "calculation_status": { "type": "keyword" },
      "calculated_at": { "type": "date" },
      "activated": { "type": "boolean" },
      "priority_score": { 
        "type": "double",
        "script": {
          "source": "doc['ad_rank_score'].value * doc['exposure_weight'].value * (1 + doc['random_weight'].value)"
        }
      }
    }
  },
  "aliases": {
    "ad_rankings": {}
  }
}
```

**체크리스트**:
- [ ] 인덱스 템플릿 생성 완료
- [ ] 매핑 설정 검증
- [ ] 샤드/레플리카 설정 확인
- [ ] Alias 설정 확인

### **✅ 2. 초기 인덱스 생성**

**Active 인덱스 생성**:
```bash
# 초기 Active 인덱스 생성
PUT ad_rankings_active_20240115_v1

# Alias 설정
POST _aliases
{
  "actions": [
    {
      "add": {
        "index": "ad_rankings_active_20240115_v1",
        "alias": "ad_rankings"
      }
    }
  ]
}
```

**Temp 인덱스 생성**:
```bash
# Temp 인덱스 생성 (replica 0으로 설정)
PUT ad_rankings_temp
{
  "settings": {
    "number_of_shards": 4,
    "number_of_replicas": 0,
    "refresh_interval": "30s"
  }
}
```

**성과 캐시 인덱스 생성**:
```bash
PUT ad_performance_cache
{
  "settings": {
    "number_of_shards": 2,
    "number_of_replicas": 1,
    "refresh_interval": "5s"
  },
  "mappings": {
    "properties": {
      "campaign_id": { "type": "long" },
      "product_id": { "type": "long" },
      "placement": { "type": "keyword" },
      "category": { "type": "keyword" },
      "window_start": { "type": "date" },
      "window_end": { "type": "date" },
      "impression_count": { "type": "long" },
      "click_count": { "type": "long" },
      "conversion_count": { "type": "long" },
      "ctr": { "type": "double" },
      "cvr": { "type": "double" },
      "total_revenue": { "type": "double" },
      "avg_cpc": { "type": "double" },
      "performance_score": { "type": "double" },
      "updated_at": { "type": "date" }
    }
  }
}
```

**체크리스트**:
- [ ] Active 인덱스 생성 완료
- [ ] Temp 인덱스 생성 완료
- [ ] 성과 캐시 인덱스 생성 완료
- [ ] Alias 설정 확인
- [ ] 인덱스 상태 확인 (Green)

---

## 🔄 하이브리드 아키텍처 구현

### **✅ 3. Alias 관리 시스템**



### **✅ 4. 실시간 이벤트 처리**

**예산 소진 시 즉시 처리**:
```java
@Service
public class RealtimeEventProcessor {
    
    @Autowired
    private ElasticsearchClient client;
    
    public void handleBudgetExhaustion(BudgetExhaustionEvent event) {
        try {
            // Active 인덱스 즉시 업데이트
            UpdateByQueryRequest request = new UpdateByQueryRequest("ad_rankings");
            
            BoolQueryBuilder query = QueryBuilders.boolQuery()
                .must(QueryBuilders.termQuery("campaign_id", event.getCampaignId()))
                .must(QueryBuilders.termQuery("activated", true));
            
            request.setQuery(query);
            
            // Script로 상태 업데이트
            Script script = new Script(ScriptType.INLINE, "painless", 
                "ctx._source.activated = false; ctx._source.calculation_status = 'PAUSED'", 
                Collections.emptyMap());
            request.setScript(script);
            
            BulkByScrollResponse response = client.updateByQuery(request);
            
            log.info("Budget exhaustion processed: {} documents updated", response.getUpdated());
            
        } catch (IOException e) {
            log.error("Budget exhaustion processing failed", e);
        }
    }
}
```

**체크리스트**:
- [ ] 실시간 이벤트 처리기 구현
- [ ] UpdateByQuery 로직 구현
- [ ] Script 기반 상태 업데이트
- [ ] 에러 처리 및 로깅
- [ ] 처리 결과 검증

---

## 🎯 적응형 조회 전략 구현

### **✅ 5. 적응형 조회 엔진**

**데이터 크기별 조회 방식 선택**:
```java
@Service
public class AdaptiveAdRankService {
    
    @Autowired
    private AdRankCalculator calculator;
    
    @Autowired
    private RedisTemplate<String, Object> redisTemplate;
    
    @Autowired
    private ElasticsearchClient elasticsearchClient;
    
    public List<AdRankResult> getAdRankings(String placement, String category, String searchTerm) {
        // 1. 데이터 크기 확인
        int productCount = getProductCount(placement, category);
        
        if (productCount < 100) {
            // 실시간 계산
            return calculateRealtime(placement, category, searchTerm);
        } else if (productCount < 1000) {
            // Redis 캐시
            return getFromRedisCache(placement, category, searchTerm);
        } else {
            // Elasticsearch O(1) 조회
            return getFromElasticsearch(placement, category, searchTerm);
        }
    }
    
    private List<AdRankResult> calculateRealtime(String placement, String category, String searchTerm) {
        // DB에서 필요한 데이터 조회
        List<CampaignProduct> products = getActiveProducts(placement, category);
        
        // 실시간 AD_RANK 계산
        List<AdRankResult> results = products.stream()
            .map(product -> calculator.calculateAdRank(product, placement, category))
            .filter(result -> result.getAdRankScore() > 0)
            .sorted(Comparator.comparing(AdRankResult::getAdRankScore).reversed())
            .limit(10)
            .collect(Collectors.toList());
        
        // 선택적 캐싱
        if (shouldCache(placement, category, searchTerm)) {
            cacheResults(placement, category, searchTerm, results);
        }
        
        return results;
    }
    
    private List<AdRankResult> getFromRedisCache(String placement, String category, String searchTerm) {
        String cacheKey = generateCacheKey(placement, category, searchTerm);
        String cachedResult = (String) redisTemplate.opsForValue().get(cacheKey);
        
        if (cachedResult != null) {
            return deserializeResults(cachedResult);
        }
        
        // 캐시 미스 시 Elasticsearch 조회
        List<AdRankResult> results = getFromElasticsearch(placement, category, searchTerm);
        
        // Redis에 캐싱 (TTL: 30초)
        redisTemplate.opsForValue().set(cacheKey, serializeResults(results), Duration.ofSeconds(30));
        
        return results;
    }
    
    private List<AdRankResult> getFromElasticsearch(String placement, String category, String searchTerm) {
        SearchRequest searchRequest = new SearchRequest("ad_rankings");
        SearchSourceBuilder sourceBuilder = new SearchSourceBuilder();
        
        // 쿼리 구성
        BoolQueryBuilder boolQuery = QueryBuilders.boolQuery()
            .must(QueryBuilders.termQuery("placement", placement))
            .must(QueryBuilders.termQuery("activated", true))
            .must(QueryBuilders.termQuery("calculation_status", "CALCULATED"))
            .filter(QueryBuilders.rangeQuery("wallet_balance").gt(0))
            .filter(QueryBuilders.rangeQuery("daily_budget").gt("current_spent"));
        
        if (category != null) {
            boolQuery.must(QueryBuilders.termQuery("category_path", category));
        }
        
        if (searchTerm != null) {
            boolQuery.should(QueryBuilders.matchQuery("search_term", searchTerm));
        }
        
        sourceBuilder.query(boolQuery);
        sourceBuilder.sort("priority_score", SortOrder.DESC);
        sourceBuilder.size(10);
        sourceBuilder.fetchSource(new String[]{"campaign_id", "product_id", "ad_rank_score", "cpc_price", "exposure_weight"}, null);
        
        searchRequest.source(sourceBuilder);
        
        try {
            SearchResponse response = elasticsearchClient.search(searchRequest);
            return parseAdRankResults(response);
        } catch (IOException e) {
            log.error("Elasticsearch query error", e);
            return Collections.emptyList();
        }
    }
}
```

**체크리스트**:
- [ ] 적응형 조회 서비스 구현
- [ ] 데이터 크기 확인 로직
- [ ] 실시간 계산 엔진
- [ ] Redis 캐시 연동
- [ ] Elasticsearch O(1) 조회
- [ ] 선택적 캐싱 로직
- [ ] 에러 처리 및 폴백

### **✅ 6. 지능형 캐싱 매니저**

**사용 패턴 기반 캐싱**:
```java
@Component
public class IntelligentCacheManager {
    
    private final Map<String, CacheStats> cacheStats = new ConcurrentHashMap<>();
    
    public boolean shouldCache(String placement, String category, String searchTerm) {
        String key = generateKey(placement, category, searchTerm);
        CacheStats stats = cacheStats.get(key);
        
        if (stats == null) {
            return false; // 첫 조회는 캐싱 안함
        }
        
        // 조회 빈도가 높고, 데이터 크기가 적당한 경우만 캐싱
        return stats.getAccessCount() > 5 && 
               stats.getDataSize() >= 50 && 
               stats.getDataSize() <= 500;
    }
    
    public void updateCacheStats(String placement, String category, String searchTerm, int dataSize) {
        String key = generateKey(placement, category, searchTerm);
        cacheStats.compute(key, (k, v) -> {
            if (v == null) {
                return new CacheStats(1, dataSize, System.currentTimeMillis());
            }
            return v.incrementAccess(dataSize);
        });
    }
    
    public void cleanupOldStats() {
        long cutoffTime = System.currentTimeMillis() - (24 * 60 * 60 * 1000); // 24시간
        cacheStats.entrySet().removeIf(entry -> entry.getValue().getLastAccessTime() < cutoffTime);
    }
}
```

**체크리스트**:
- [ ] 캐시 통계 관리자 구현
- [ ] 캐싱 판단 로직
- [ ] 통계 업데이트 로직
- [ ] 오래된 통계 정리
- [ ] 메모리 사용량 최적화

---

## 🚀 성능 최적화

### **✅ 7. Bulk API 최적화**

**Worker AD_RANK 계산 후 벌크 업데이트**:
```java
@Service
public class AdRankElasticsearchService {
    
    @Autowired
    private ElasticsearchClient client;
    
    public void updateAdRankings(List<AdRankCalculation> calculations) {
        BulkRequest bulkRequest = new BulkRequest();
        
        for (AdRankCalculation calc : calculations) {
            IndexRequest request = new IndexRequest("ad_rankings_temp")
                .id(calc.getCampaignId() + "_" + calc.getProductId())
                .source(Map.of(
                    "campaign_id", calc.getCampaignId(),
                    "product_id", calc.getProductId(),
                    "placement", calc.getPlacement(),
                    "category_path", calc.getCategoryPath(),
                    "ad_rank_score", calc.getAdRankScore(),
                    "cpc_price", calc.getCpcPrice(),
                    "exposure_rank", calc.getExposureRank(),
                    "exposure_weight", calc.getExposureWeight(),
                    "random_weight", calc.getRandomWeight(),
                    "click_conversion_rate", calc.getClickConversionRate(),
                    "max_bid_price", calc.getMaxBidPrice(),
                    "daily_budget", calc.getDailyBudget(),
                    "current_spent", calc.getCurrentSpent(),
                    "wallet_balance", calc.getWalletBalance(),
                    "campaign_status", calc.getCampaignStatus(),
                    "calculation_status", "CALCULATED",
                    "calculated_at", new Date(),
                    "activated", true
                ));
            bulkRequest.add(request);
        }
        
        try {
            BulkResponse response = client.bulk(bulkRequest);
            if (response.hasFailures()) {
                log.error("Bulk update failed: {}", response.buildFailureMessage());
            }
        } catch (IOException e) {
            log.error("Elasticsearch bulk update error", e);
        }
    }
}
```

**체크리스트**:
- [ ] Bulk API 구현
- [ ] 벌크 크기 최적화 (1000개씩)
- [ ] 벌크 간격 설정 (100ms)
- [ ] 에러 처리 및 재시도
- [ ] 성능 모니터링

### **✅ 8. 쿼리 최적화**

**최적화된 쿼리 템플릿**:
```java
@Component
public class OptimizedQueryTemplates {
    
    public SearchRequest createAdRankQuery(String placement, String category, String searchTerm) {
        SearchRequest searchRequest = new SearchRequest("ad_rankings");
        SearchSourceBuilder sourceBuilder = new SearchSourceBuilder();
        
        // 최적화된 쿼리 구성
        BoolQueryBuilder boolQuery = QueryBuilders.boolQuery()
            .must(QueryBuilders.termQuery("placement", placement))
            .must(QueryBuilders.termQuery("activated", true))
            .must(QueryBuilders.termQuery("calculation_status", "CALCULATED"))
            .filter(QueryBuilders.rangeQuery("wallet_balance").gt(0))
            .filter(QueryBuilders.rangeQuery("daily_budget").gt("current_spent"));
        
        if (category != null) {
            boolQuery.must(QueryBuilders.termQuery("category_path", category));
        }
        
        if (searchTerm != null) {
            boolQuery.should(QueryBuilders.matchQuery("search_term", searchTerm));
        }
        
        sourceBuilder.query(boolQuery);
        sourceBuilder.sort("priority_score", SortOrder.DESC);
        sourceBuilder.size(10);
        
        // 필요한 필드만 조회
        sourceBuilder.fetchSource(new String[]{
            "campaign_id", "product_id", "ad_rank_score", 
            "cpc_price", "exposure_weight"
        }, null);
        
        searchRequest.source(sourceBuilder);
        return searchRequest;
    }
}
```

**체크리스트**:
- [ ] 쿼리 템플릿 구현
- [ ] 필드 선택 최적화
- [ ] 정렬 최적화
- [ ] 쿼리 캐시 활용
- [ ] 성능 테스트

---

## 📈 운영 및 모니터링

### **✅ 9. 클러스터 모니터링**



**체크리스트**:
- [ ] 클러스터 헬스 모니터링
- [ ] 노드 상태 모니터링
- [ ] CPU/메모리 사용량 모니터링
- [ ] 알림 시스템 연동
- [ ] 로깅 및 대시보드

### **✅ 10. 성능 메트릭 수집**


            log.error("Metrics collection failed", e);
        }
    }
}
```

**체크리스트**:
- [ ] 성능 메트릭 수집기 구현
- [ ] 검색 통계 수집
- [ ] 인덱싱 통계 수집
- [ ] 메트릭 서비스 연동
- [ ] 대시보드 구성

---

## 💾 백업 및 복구

### **✅ 11. 스냅샷 백업**



**체크리스트**:
- [ ] 스냅샷 저장소 설정
- [ ] 자동 백업 스케줄러
- [ ] 스냅샷 생성 로직
- [ ] 백업 검증
- [ ] 오래된 스냅샷 정리

### **✅ 12. 복구 시스템**

**스냅샷 복구**:
```java
@Service
public class ElasticsearchRecoveryService {
    
    @Autowired
    private ElasticsearchClient client;
    
    public void restoreFromSnapshot(String snapshotName) {
        try {
            // 복구 전 기존 인덱스 삭제
            DeleteIndexRequest deleteRequest = new DeleteIndexRequest("ad_rankings_*");
            client.indices().delete(deleteRequest);
            
            // 스냅샷에서 복구
            RestoreSnapshotRequest restoreRequest = new RestoreSnapshotRequest();
            restoreRequest.repository("ad_rankings_backup");
            restoreRequest.snapshot(snapshotName);
            restoreRequest.indices("ad_rankings_*");
            restoreRequest.ignoreUnavailable(true);
            restoreRequest.includeGlobalState(false);
            
            RestoreSnapshotResponse response = client.snapshot().restore(restoreRequest);
            
            log.info("Recovery completed: {} indices restored", response.getRestoreInfo().indices().size());
            
        } catch (IOException e) {
            log.error("Recovery failed", e);
        }
    }
}
```

**체크리스트**:
- [ ] 복구 서비스 구현
- [ ] 기존 인덱스 정리
- [ ] 스냅샷 복구 로직
- [ ] 복구 검증
- [ ] 에러 처리

---

## 📋 최종 검증 체크리스트

### **✅ 13. 전체 시스템 검증**

**기능 검증**:
- [ ] 적응형 조회 전략 정상 동작
- [ ] 하이브리드 아키텍처 정상 동작
- [ ] 실시간 업데이트 정상 동작
- [ ] 배치 업데이트 정상 동작
- [ ] 캐싱 시스템 정상 동작

**성능 검증**:
- [ ] 전체 평균 응답시간 < 5ms 달성
- [ ] 6000 QPS 처리량 달성
- [ ] 캐시 히트율 85% 이상 달성
- [ ] 메모리 사용량 최적화
- [ ] CPU 사용률 70% 이하

**안정성 검증**:
- [ ] 장애 복구 테스트
- [ ] 백업/복구 테스트
- [ ] 부하 테스트
- [ ] 장시간 운영 테스트
- [ ] 모니터링 시스템 검증

**보안 검증**:
- [ ] 인증/인가 설정
- [ ] 네트워크 보안 설정
- [ ] 데이터 암호화 설정
- [ ] 접근 로그 모니터링
- [ ] 보안 정책 준수

---

이 체크리스트를 통해 AD_RANK 시스템의 Elasticsearch 구현을 체계적으로 완성할 수 있습니다. 